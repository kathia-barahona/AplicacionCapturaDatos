<p id="notice"><%= notice %></p>

<h1>Listing Entries</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Type</th>
      <th>Channel</th>
      <th>H inicio</th>
      <th>H final</th>
      <th>Location</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @registros.each do |registro| %>
      <tr>
        <td><%= registro.type %></td>
        <td><%= registro.channel %></td>
        <td><%= registro.h_inicio %></td>
        <td><%= registro.h_final %></td>
        <td><%= registro.location %></td>
        <td><%= link_to 'Show', registro %></td>
        <td><%= link_to 'Edit', edit_registro_path(registro) %></td>
        <td><%= link_to 'Delete', registro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


